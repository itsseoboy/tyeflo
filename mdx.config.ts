const mdxConfig = {
  // This file configures MDX rendering for the project.
  // You can add remark/rehype plugins here if needed.
  // For now, default MDX behavior is sufficient.
};

export default mdxConfig;
